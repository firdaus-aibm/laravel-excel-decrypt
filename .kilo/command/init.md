---
description: Initialize the development environment
---
Check PHP version (required ^8.2), run `composer install`, then print a summary with PHP version, installed package count, and Laravel version constraint from composer.json.
